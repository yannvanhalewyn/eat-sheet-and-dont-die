(ns sheet-bucket.models.chord-test
  (:require [sheet-bucket.models.chord :as subject]
            [cljs.test :refer-macros [deftest is testing]]))

(deftest foo
  (testing "Hello")
  (is (= true true)))
